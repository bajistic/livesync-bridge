services:
  bridge:
    build: .
    volumes:
      - /home/bajistic/clawd:/app/data/ivault
      - ./dat:/app/dat
    network_mode: host
    restart: unless-stopped
    command: ["deno", "run", "-A", "main.ts"]
